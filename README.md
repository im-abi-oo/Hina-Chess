♟️ Hina Chess Pro v5.0Hina Chess Pro یک پلتفرم شطرنج آنلاین مدرن، سریع و ریسپانسیو است که با هدف ارائه تجربه‌ای حرفه‌ای از بازی شطرنج در محیط وب توسعه یافته است. این پروژه از تکنولوژی‌های روز دنیا برای تضمین سرعت بالا و ارتباط بدون تاخیر (Real-time) استفاده می‌کند.✨ ویژگی‌های کلیدی🎮 تجربه بازیبازی آنلاین بدون وقفه: استفاده از WebSockets برای انتقال حرکات در کمتر از چند میلی‌ثانیه.سیستم نوبت‌دهی هوشمند: مدیریت دقیق نوبت‌ها و جلوگیری از حرکات غیرمجاز با استفاده از کتابخانه chess.js.تایمر حرفه‌ای: سیستم ساعت دوگانه برای هر بازیکن با قابلیت اتمام بازی در صورت پایان زمان.شخصی‌سازی ظاهری: قابلیت تغییر تم صفحه شطرنج (تیره/روشن) و مهره‌ها به سلیقه کاربر.👥 بخش اجتماعی و لابیسیستم احراز هویت (Auth): ثبت‌نام و ورود امن با استفاده از JWT و کوکی‌های امن.لابی زنده: مشاهده تمامی اتاق‌های فعال، تعداد بازیکنان و پیوستن سریع به بازی‌ها.چت زنده: قابلیت گفتگو با حریف در محیط بازی با فیلتر امنیتی XSS.سیستم دوستان (Social): قابلیت ارسال درخواست دوستی و چت خصوصی (در نسخه‌های حرفه‌ای).🛠 فنی و زیرساختیFull Stack Next.js: بهره‌گیری از قدرت SSR و API Routes در کنار یک سرور Express سفارشی.دیتابیس MongoDB: ذخیره‌سازی اطلاعات کاربران، سوابق بازی‌ها و امتیازات (ELO).PWA Ready: قابلیت نصب روی موبایل و دسکتاپ به عنوان یک اپلیکیشن مستقل.طراحی ریسپانسیو: بهینه‌سازی کامل برای موبایل، تبلت و دسکتاپ.🚀 تکنولوژی‌های استفاده شدهFrontend: Next.js, React, Tailwind CSSBackend: Node.js, ExpressReal-time: Socket.ioDatabase: MongoDB با ODM MongooseLogic: Chess.js & React-ChessboardSecurity: JSON Web Tokens, Bcrypt.js📦 راهنمای نصب و اجراپیش‌نیازهانصب بودن Node.js (نسخه ۱۶ به بالا)نصب بودن و در حال اجرا بودن MongoDBمراحل اجرا۱. ابتدا مخزن را کلون کنید یا فایل‌ها را استخراج کنید.۲. وابستگی‌ها را نصب کنید:npm install
۳. فایل .env را در روت پروژه ایجاد کرده و متغیرهای زیر را تنظیم کنید:MONGODB_URI=mongodb://localhost:27017/hina_chess
JWT_SECRET=your_secret_key
PORT=3000
۴. پروژه را در حالت توسعه اجرا کنید:npm run dev
سپس مرورگر خود را باز کرده و به آدرس http://localhost:3000 بروید.📁 ساختار پوشه‌بندی├── lib/               # منطق بک‌انده، مدل‌ها و اتصال دیتابیس
│   ├── db.js          # اتصال به MongoDB
│   ├── models.js      # مدل‌های User و Message
│   ├── rooms.js       # مدیریت حافظه موقت اتاق‌ها
│   └── socket.js      # کنترلر اصلی سوکت و منطق بازی
├── pages/             # صفحات Next.js و API Routes
│   ├── api/           # اندپوینت‌های احراز هویت
│   ├── game/[id].js   # صفحه اصلی محیط بازی
│   ├── auth.js        # صفحه ورود و ثبت‌نام
│   └── dashboard.js   # لابی و مدیریت حساب
├── public/            # فایل‌های استاتیک، صداها و آیکون‌ها
├── styles/            # فایل‌های CSS و تم‌های کلی
└── server.js          # نقطه شروع (Entry Point) سرور سفارشی
👨‍💻 توسعه‌دهندهطراحی و پیاده‌سازی توسط im_abi.نسخه ۵.۰ پرو - ۲۰۲۶
